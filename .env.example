# ---- 面板 Panel ----
SITE_NAME=Homing
ADMIN_USER=admin
ADMIN_PASS=change-me-please
# Flask 会话密钥：设一个长随机串，否则每次重启登录态失效
# 生成：python3 -c "import secrets;print(secrets.token_hex(32))"
SECRET_KEY=

# ---- 可选 Optional ----
# 默认 zone，留空则从主机名自动推断；Cloudflare Token 在面板里添加，不放这里
CF_ZONE_NAME=
